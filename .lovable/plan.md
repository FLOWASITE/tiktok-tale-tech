

## Hạ ngưỡng Brand Positioning trong tính completeness

**File:** `src/utils/brandCompleteness.ts`

**Thay đổi:** Dòng kiểm tra `brand_positioning` từ `trim().length > 10` xuống `trim().length > 0` — chỉ cần có nội dung là tính hoàn thành.

